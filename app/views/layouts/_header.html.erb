<div class="navbar navbar-fluid-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "Photocopy Work Pool", root_path, class: "brand"  %>
      <div class="container-fluid nav-collapse">
        <ul class="nav">
          <li><%= link_to "Mis trabajos", root_path  %></li>
        </ul>
        <ul class="nav pull-right">
            <li>
              <% if user_signed_in? %>
                <%= link_to "Cerrar sesión " + current_user.email, destroy_user_session_path %>
              <% else %>
                <%= link_to "Iniciar sesión", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
              <% end %>
            </li>
          </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>