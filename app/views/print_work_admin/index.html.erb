<%- model_class = PrintWork -%>
<div class="page-header">
  <h1><%=t '.title', :default => t("helpers.models.print_work.index") %></h1>
</div>
<% unless @print_works.empty? %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= model_class.human_attribute_name(:file) %></th>
        <th><%= model_class.human_attribute_name(:description) %></th>
        <th><%= model_class.human_attribute_name(:state) %></th>
        <th><%= model_class.human_attribute_name(:user) %></th>
        <th><%=t '.actions', :default => t("helpers.actions") %></th>
      </tr>
    </thead>
    <tbody>
      <% @print_works.each do |print_work| %>
        <tr>
          <td><%= link_to print_work.file.name, print_work.file.to_s %></td>
          <td><%= print_work.description %></td>
          <td><%= raw print_work.state.text %></td>
          <td><%= print_work.user.email %></td>
          <td>
            <%= link_to t('.edit', :default => t("helpers.links.edit")),
                        edit_print_work_admin_path(print_work), :class => 'btn btn-mini' %>
            <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                        print_work_admin_path(print_work),
                        :method => :delete,
                        :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                        :class => 'btn btn-mini btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="alert alert-success">No subieron ningún trabajo aún..</div>
<% end %>
